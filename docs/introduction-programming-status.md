# Introduction to Programming — Step 8

**IN PROGRESS — acceptance has not yet been claimed.** Started from clean `bda8c0f` on 9 September 2026. The supplemental enrichment patch is preserved. Step 9 is out of scope.

## Baseline checkpoint

All sixteen requested baseline commands passed freshly before authored edits. `docs/ip-baseline-results.json` records commands, exit codes and timings. Vitest: **1,029 tests / 40 files**. Typecheck, production build and all content, projection, CO/RL/enrichment and bundle gates passed. Bundle reports were regenerated by their existing inspection commands. No original working changes were discarded.

Canonical scope confirmed directly from v1.0.1: **20 topics / 44 subtopics / 56 skills**, subject `CSE1100_IP`. Seven assessments retain integrated mapping policy; 38 analytical component locators remain UNKNOWN. Published T02 pilot and all accepted engine/storage/course content are protected.

## Continuation checkpoint

The user resumed while HEAD was still `bda8c0f`; there was no checkpoint commit and nothing staged. Valid uncommitted work was preserved. Before continuation: 19 authored lesson files, 33 fixed Java prediction definitions and shared registry/feedback extension, workbench components, source review and initial trusted-Java checking. Assignment/reference authoring and validation were still active. An incomplete integration expression was corrected during reconciliation; no protected code was rewritten.

Now implemented: 20 lessons including the unchanged T02 pilot; 118 blocks, 112 cards (two per canonical skill), 21 guided activities, 35 fixed predictions including two published items, eight optional map cues, eleven coding tasks (5 × 15, 3 × 30, 3 × 60 minutes), 33 shared feedback profiles. Content validation passes 20/44/56 coverage. UI routes, ephemeral multi-file editing/copy/reset/reference/specification reveal, source/lock checks and course summaries are connected. Student Schema 1 / DB1 and all protected baseline records remain unchanged.

All source review is recorded in `ip-source-review.md`; raw sources remain outside the repository. The complete topic/skill/task mapping is in `ip-coverage-inventory.md`. Trusted authored Java checking has run, and will run again as part of final acceptance. Tests report mocked storage separately from real-browser acceptance.

## Open acceptance work

- Finish full initial automated suite and production bundle checks.
- Perform the distinct post-green adversarial self-review and fix confirmed defects with regression evidence.
- Run isolated production Chromium acceptance at actual 390/768/1280 CSS pixels, including CO/R&L and all three Assembly examples.
- Run every final acceptance command freshly, finish evidence tables/limitations and commit the accepted result.

No Step 8 completion or browser PASS is claimed yet. Step 9 remains out of scope.

## Buildable implementation checkpoint

Initial complete suite: 1,239 tests in 45 files passed before the distinct adversarial review. Review findings were reproduced and fixed with additional regression tests; see `ip-adversarial-review.md`. The new production IP bundle gate now passes: 19 independent topic chunks, 11 assignment chunks and 11 reference chunks. Initial JavaScript is 481,622 bytes (126,323 gzip), an increase of 6,545 bytes (1,219 gzip) over the accepted baseline. Final all-gate rerun and the remaining browser interactions are still required before acceptance. This implementation checkpoint is resumable; it is not a Step 8 completion claim.
