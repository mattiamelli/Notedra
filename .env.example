# Optional. Without these, DelftStudy remains fully local.
# Use only the project origin and a browser-safe publishable/legacy anon key.
# Never put service-role, admin, database-password or signing-secret values here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
