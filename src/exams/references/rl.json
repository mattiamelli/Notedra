[
  {
    "itemId": "ds.exam.rl.formalization",
    "version": "1",
    "reference": "With I(x), T(y), R(y,x), use ∀x((I(x)∧∃y R(y,x))→∃y(T(y)∧R(y,x))) and ¬∃y(T(y)∧∀x(I(x)→R(y,x))). If there are no instruments, the second clause rules out technicians under the usual vacuous truth convention. Equivalent formulations are accepted by rubric, not string comparison.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.quantifiers",
    "version": "1",
    "reference": "∃x∀y(A(x)∧(¬B(y)∨¬S(x,y))). There must be an x satisfying A for which no y satisfies both B and S(x,y). Parenthesization and logically equivalent forms are welcome.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.countermodel",
    "version": "1",
    "reference": "Domain {a,b}, T={(a,a),(b,b)} works. Each x has itself as witness. Neither possible y is related from both x values. Many other finite structures work.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.proof-method",
    "version": "1",
    "reference": "Write n=2k+1. Then n²=4k(k+1)+1. One of k,k+1 is even (cases on parity), so k(k+1)=2m for some integer m, giving n²=8m+1. The argument uses generalization and a parity case split.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.induction",
    "version": "1",
    "reference": "Base n=0: 8−5=3. For arbitrary k≥0 assume d(k)=8·2^k−5. Then d(k+1)=2(8·2^k−5)+5=8·2^(k+1)−5. The step is ∀k≥0(P(k)→P(k+1)); conclude all n≥0.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.invariant",
    "version": "1",
    "reference": "Sum + length is invariant: sum grows by 1 while length drops by 1. Initially S+n; at termination length=1, so final value=S+n−1. Length is a positive integer and decreases every step, proving termination.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.recursive-set",
    "version": "1",
    "reference": "Base: ε has zero of each. Wrapping adds one of each to equal counts. Concatenation adds two pairs of equal counts. By structural induction all words generated by the rules are balanced in count (this claim alone does not characterize valid nesting).",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.sets",
    "version": "1",
    "reference": "For arbitrary x, left membership means x∈A and x∉B and x∉C, exactly membership of both differences. Counterexample A={r,s}, B={r}, C={s}: left is empty while the union of differences is A.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.relations",
    "version": "1",
    "reference": "Reflexive difference 0=7·0; symmetry negates the integer multiple; transitivity adds multiples. f is surjective via representatives 0,…,6, but not injective since 0 and 7 share an image; thus not bijective.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.tree",
    "version": "1",
    "reference": "Empty tree and leaf give 0. For an internal node with k children, return (1 if k is odd else 0)+the sum of child results. The specified tree yields 1: only its root qualifies.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.graph",
    "version": "1",
    "reference": "Edges h→j, h→k, j→m, k→m, m→n work. The orders are h,j,k,m,n and h,k,j,m,n. h precedes both middle vertices, both precede m, and n is last. The only freedom is j versus k.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  },
  {
    "itemId": "ds.exam.rl.transfer",
    "version": "1",
    "reference": "Possible U,V placements are (1,2),(2,3),(3,4). The last leaves no later slot for W. The middle leaves W only slot4, forbidden. The first forces W=3 and X=4. Unique schedule U,V,W,X.",
    "reasoning": "Use the requirements as separate checks. Other valid constructions are welcome. Self-review records workflow only; it does not verify correctness."
  }
]
