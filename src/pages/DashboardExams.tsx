import {useMemo,useState,type FormEvent} from 'react';
import {ShellIcon} from '../shell/ShellIcon';
import {useLearning} from '../learning/LearningProvider';
import type {UpcomingExam} from '../learning/contracts';
export function calendarDays(from:string,to:string){const a=from.split('-').map(Number),b=to.split('-').map(Number);return Math.round((Date.UTC(b[0],b[1]-1,b[2])-Date.UTC(a[0],a[1]-1,a[2]))/86400000)}
export function urgency(days:number){return days<=2?'critical':days<=7?'high':days<=14?'medium':days<=30?'low':'neutral'}
export default function DashboardExams(){const learning=useLearning(),exams=learning?.snapshot?.data.upcomingExams??[],[editing,setEditing]=useState<UpcomingExam|null>(null),[open,setOpen]=useState(false);const today=new Date().toISOString().slice(0,10);const sorted=useMemo(()=>exams.map(e=>({...e,days:calendarDays(today,e.examDate)})).filter(e=>e.days>=0).sort((a,b)=>a.days-b.days),[exams,today]);const save=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!learning?.snapshot)return;const f=new FormData(e.currentTarget),v={id:editing?.id??crypto.randomUUID(),name:String(f.get('name')??''),examDate:String(f.get('date')??'')};await learning.changeStudentData(r=>editing?r.updateUpcomingExam(v,learning.snapshot!.data):r.addUpcomingExam(v,learning.snapshot!.data));setOpen(false);setEditing(null)};const remove=async(id:string)=>{if(!learning?.snapshot||!confirm('Delete this exam?'))return;await learning.changeStudentData(r=>r.deleteUpcomingExam(id,learning.snapshot!.data))};return <section className="ds-dash-panel ds-dash-exams"><div className="ds-section-heading"><h2><ShellIcon name="calendar"/>Upcoming Exams</h2><button className="ds-button" onClick={()=>{setEditing(null);setOpen(true)}}>+ Add exam</button></div>{sorted.length===0?<p className="ds-dash-caption">Add an exam date to keep it in view.</p>:<ul className="ds-upcoming-list">{sorted.slice(0,3).map(e=><li key={e.id}><div><strong>{e.name}</strong><small>{e.examDate}</small></div><span className={`ds-countdown ds-urgency-${urgency(e.days)}`}>{e.days===0?'TODAY':e.days===1?'TOMORROW':`${e.days} DAYS`}</span><button onClick={()=>{setEditing(e);setOpen(true)}}>Edit</button><button onClick={()=>void remove(e.id)}>Delete</button></li>)}</ul>}{open&&<div role="dialog" aria-modal="true"><form onSubmit={save}><label>Exam name<input name="name" required maxLength={120} defaultValue={editing?.name??''}/></label><label>Exam date<input type="date" name="date" required defaultValue={editing?.examDate??''}/></label><button type="submit">Save</button><button type="button" onClick={()=>setOpen(false)}>Cancel</button></form></div>}</section>}
